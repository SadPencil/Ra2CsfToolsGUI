<TabItem Header="Translation Helper - Override" x:Class="Ra2CsfToolsGUI.TranslationHelperOverrideTabItem"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Ra2CsfToolsGUI"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
>
    <ui:TabItemHelper.Icon>
        <ui:SymbolIcon Symbol="Comment" />
    </ui:TabItemHelper.Icon>

    <ui:SimpleStackPanel Spacing="12" Margin="10">
        <TextBlock TextWrapping="Wrap" FontSize="14">Select an upstream string table file and a translated string label file, and save an .ini file that once a label is shown in the translated file, the value is selected. Otherwise, the label shown in the upstream file is selected. The .ini file can be converted back to .csf file in the "Format Converter" tab.</TextBlock>

        <Button Content="Load an upstream string table file (.csf/.ini) ..." x:Name="LoadUpstreamFileButton" />
        <Button Content="Load a translated string table file (.csf/.ini) ..." x:Name="LoadTranslatedFileButton" />
        <Button Content="Save as an .ini file for comparison ..." x:Name="SaveIniFileButton" />
    </ui:SimpleStackPanel>
</TabItem>
